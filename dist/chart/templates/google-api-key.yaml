apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.fullname" . }}-google-api-key
  labels:
  {{- include "chart.labels" . | nindent 4 }}
stringData:
  GOOGLE_API_KEY: {{ required "googleApiKey is required" .Values.googleApiKey
    | quote }}
type: Opaque
