apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "khieron.fullname" . }}-agent-instruction
  labels:
  {{- include "khieron.labels" . | nindent 4 }}
data:
  instruction.txt: {{ .Values.agentInstruction.instructionTxt | toYaml | indent 1
    }}
