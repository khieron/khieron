apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "khieron.fullname" . }}-skill-viewer-role
  labels:
    rbac.authorization.k8s.io/aggregate-to-view: "true"
  {{- include "khieron.labels" . | nindent 4 }}
rules:
- apiGroups:
  - agency.khieron.io
  resources:
  - skills
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - agency.khieron.io
  resources:
  - skills/status
  verbs:
  - get
