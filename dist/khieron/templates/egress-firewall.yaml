{{- if and .Values.egressFirewall .Values.egressFirewall.enabled }}
apiVersion: k8s.ovn.org/v1
kind: EgressFirewall
metadata:
  name: default
  labels:
  {{- include "khieron.labels" . | nindent 4 }}
spec:
  egress:
  - to:
      dnsName: {{ .Values.egressFirewall.modelDnsName}}
    type: Allow
  - to:
      cidrSelector: {{ .Values.egressFirewall.clusterCIDR }}
    type: Allow
  {{- if .Values.egressFirewall.serviceCIDR }}
  - to:
      cidrSelector: {{ .Values.egressFirewall.serviceCIDR }}
    type: Allow
  {{- end }}
  {{- if .Values.egressFirewall.nodeNetworkCIDR }}
  - to:
      cidrSelector: {{ .Values.egressFirewall.nodeNetworkCIDR }}
    type: Allow
  {{- end }}
  - to:
      cidrSelector: 0.0.0.0/0
    type: Deny
{{- end }}
