apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "skill.fullname" . }}
  labels:
    {{- include "skill.labels" . | nindent 4 }}
rules: [] # Add rules relevant to the access required in scripts
