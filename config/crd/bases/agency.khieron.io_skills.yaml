---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: skills.agency.khieron.io
spec:
  group: agency.khieron.io
  names:
    kind: Skill
    listKind: SkillList
    plural: skills
    singular: skill
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Skill is the Schema for the skills API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SkillSpec defines the desired state of Skill.
            properties:
              enableagent:
                default: true
                type: boolean
              intervalminute:
                default: 5
                description: The analysis will run periodically at this interval,
                  or on a change of spec
                type: integer
              mcpconfigref:
                description: |-
                  MCPConfigRef is an optional reference to a ConfigMap containing MCP server
                  configuration. The ConfigMap must contain a key "mcp.json" with server
                  definitions in Claude Code .mcp.json format.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              skillconfigref:
                description: |-
                  Skill is a the name of a multifile ConfigMap of the user defined agentic skill
                  Skills should be targeted and granular
                  They should contain at least a SKILL.md and follow the Agent Skills pattern at https://agentskills.io
                  Add more Skill resources if you need them
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - enableagent
            - skillconfigref
            type: object
          status:
            description: SkillStatus defines the observed state of Skill.
            properties:
              lastanalysedat:
                description: Time of last analysis
                format: date-time
                type: string
              tokensLastRun:
                description: Token usage from the most recent run
                properties:
                  candidatesTokenCount:
                    description: Tokens in the model's response
                    format: int32
                    type: integer
                  promptTokenCount:
                    description: Tokens in the prompt
                    format: int32
                    type: integer
                  thoughtsTokenCount:
                    description: Tokens used for model reasoning
                    format: int32
                    type: integer
                  toolUsePromptTokenCount:
                    description: Tokens from tool results fed back to the model
                    format: int32
                    type: integer
                  totalTokenCount:
                    description: Total tokens (prompt + candidates + tool use + thoughts)
                    format: int32
                    type: integer
                type: object
              tokensTotal:
                description: Cumulative token usage across all runs
                properties:
                  runCount:
                    description: Number of completed agent runs
                    format: int32
                    type: integer
                  totalTokenCount:
                    description: Total tokens consumed across all runs
                    format: int64
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
