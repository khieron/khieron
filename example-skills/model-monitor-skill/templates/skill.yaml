---
apiVersion: agency.khieron.io/v1alpha1
kind: Skill
metadata:
  name: {{ include "skill.fullname" . }}
  labels:
    {{- include "skill.labels" . | nindent 4 }}
spec:
  skillconfigref:
    name: {{ include "skill.fullname" . | quote }}
  {{ if and .Values.mcpConfig .Values.mcpConfig.mcpJson }}
  mcpconfigref:
    name: {{ include "skill.fullname" . }}-mcp-json
  {{ end }}
  intervalminute: {{ .Values.skill.intervalMinute }}
  enableagent: {{ .Values.skill.enableAgent }}